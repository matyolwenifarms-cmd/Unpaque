# Public by design — compiled into the browser bundle. RLS is what protects
# data, not the secrecy of these two.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only. Set with `supabase secrets set`, never committed, never
# pasted into chat or an issue. See docs/SETUP.md.
#   ANTHROPIC_API_KEY
