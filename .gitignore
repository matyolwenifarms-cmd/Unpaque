node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
*.tsbuildinfo
coverage

# Function secrets for local serving. The example beside it is committed.
supabase/functions/.env
# Supabase CLI state: the linked project ref and pooler URL. Local to whoever
# ran `supabase link`, and not a fact about the repository.
supabase/.temp/
