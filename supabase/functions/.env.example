# Secrets for `supabase functions serve`. Copy to supabase/functions/.env,
# which is gitignored. In a deployed project these come from
# `supabase secrets set` instead — see docs/SETUP.md.
ANTHROPIC_API_KEY=
RATE_LIMIT_SALT=local-development-salt-not-a-secret
ANALYSES_PER_DAY=50
